# Backend - local API server + PostgreSQL
# Copy to .env:  cp .env.example .env

HOST=103.179.45.111
PORT=3100

# Local PostgreSQL (run `npx prisma dev` from project root first)
DATABASE_URL="postgres://postgres:postgres@localhost:51214/template1?sslmode=disable"

# Comma-separated frontend origins allowed by CORS (include full scheme)
FRONTEND_URL=http://localhost:3000,http://103.179.45.111:3000,https://meeting-schedule-dashboard.vercel.app
